var React = require('react');
var ReactDOM = require('react-dom');
var Routes = require('./Routes.jsx');

//Routes is a component that was exported and imported here
ReactDOM.render(Routes, document.getElementById('main'));
